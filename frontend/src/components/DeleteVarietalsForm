const DeleteVarietalsForm = ({ rowObject, backendURL, refeshVarietals }) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Delete
                </button>
            </form>
        </td>

    );
};

export default DeleteVarietalsForm;
