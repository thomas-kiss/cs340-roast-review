const DeleteUserForm = ({ rowObject, backendURL, refreshUsers }) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Delete
                </button>
            </form>
        </td>

    );
};

export default DeleteUserForm;