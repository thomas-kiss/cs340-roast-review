const DeleteCoffeeBeanForm = ({ rowObject, backendURL, refreshCoffeeBeans }) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Delete
                </button>
            </form>
        </td>

    );
};

export default DeleteCoffeeBeanForm;
