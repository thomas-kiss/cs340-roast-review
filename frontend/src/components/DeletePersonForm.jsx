const DeletePersonForm = ({ rowObject, backendURL, refreshPeople }) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Delete
                </button>
            </form>
        </td>

    );
};

export default DeletePersonForm;